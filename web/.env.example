# Public URL of the backend API.
# This value is inlined into the browser bundle at build time, so it must be the
# URL the *browser* uses — not an internal Docker hostname.
NEXT_PUBLIC_API_URL=http://localhost:4000
