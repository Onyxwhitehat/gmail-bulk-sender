{
  "name": "ai-bulk-email-sender",
  "version": "1.0.0",
  "private": true,
  "description": "AI-powered bulk email sender: Gmail API + Google Sheets, personalized bulk sending with live progress.",
  "workspaces": [
    "server",
    "web"
  ],
  "engines": {
    "node": ">=22.5.0"
  },
  "scripts": {
    "dev": "concurrently -n server,web -c cyan,magenta \"npm:dev:server\" \"npm:dev:web\"",
    "dev:server": "npm run dev --workspace=server",
    "dev:web": "npm run dev --workspace=web",
    "build": "npm run build --workspace=server && npm run build --workspace=web",
    "start": "concurrently -n server,web -c cyan,magenta \"npm:start:server\" \"npm:start:web\"",
    "start:server": "npm run start --workspace=server",
    "start:web": "npm run start --workspace=web",
    "typecheck": "npm run typecheck --workspace=server && npm run typecheck --workspace=web",
    "setup": "node scripts/setup.mjs"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
